. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit: run all quality gates
# Skippable with `git commit --no-verify` for emergencies only
npm run typecheck && npm run lint && npm run format:check && npm test
